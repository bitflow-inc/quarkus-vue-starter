const path = require("path");

module.exports = {
    outputDir: path.resolve(__dirname, "../src/main/resources/META-INF/resources"),
    /*
    devServer: {
        port: process.env.VUE_APP_PORT || 3000
    }
    */
}